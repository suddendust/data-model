@namespace("org.hypertrace.core.datamodel")
protocol TimestampsProtocol {
  record TimestampRecord {
    string name;
    long timestamp;
  }

  record Timestamps {
    map<TimestampRecord> records = {};
  }
}